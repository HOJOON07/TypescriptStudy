"use strict";
// let a: string;
// a = "makr"; // O
// a = 39;
let a = "mark";
//a는 "Mark" 라는 문자열로 타입이 지정되었기 때문!
// 특정한 변수,개체에 타입을 지정해주는것이 type Annotation
// :을 붙여주면 된다.
const hello = (b) => {
    //매개변수에도 타입 지정 가능 (어노테이션)
};
// hello("mark");
//문자열 못들어간다.
